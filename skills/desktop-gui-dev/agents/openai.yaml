interface:
  display_name: "Desktop GUI Dev"
  short_description: "Build, debug, package, and review Tauri desktop apps"
  default_prompt: "Use $desktop-gui-dev to scaffold, modify, debug, package, or review a Tauri desktop app, including Rust commands, frontend command wrappers, Tauri plugins and capabilities, local persistence, platform integrations, signing, updater work, and release validation."
