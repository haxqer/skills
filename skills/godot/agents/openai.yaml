interface:
  display_name: "Godot"
  short_description: "Edit scenes, export builds, and repair Godot UIDs"
  default_prompt: "Use $godot to inspect a Godot project, batch-edit scenes and UI, export builds from existing presets, create mesh libraries, or repair missing UID metadata."
